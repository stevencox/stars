# here we assign variables to particular groups

mesos_install_mode: "slave" # {master|slave|master-slave}

# Spark

spark_local_ip: "{{ ansible_default_ipv4.address }}"
spark_python_virtual_env: /projects/stars/venv3

# Zeppelin

zeppelin_service_username: "{{ stars_user }}"
zeppelin_service_group: "{{ stars_group }}"

# Blazegraph

blazegraph_service_username: "{{ stars_user }}"
blazegraph_service_group: "{{ stars_group }}"

# NodeJS

npm_config_prefix: "{{ app_root }}"
nodejs_install_npm_user: "{{ stars_user }}"


# Lightning

lightning_user: "{{ stars_user }}"
lightning_group: "{{ stars_group }}"